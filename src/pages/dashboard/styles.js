import { makeStyles } from '@material-ui/styles';

export default makeStyles((theme) => ({
  container: {
    paddingLeft: '60px',
    paddingRight: '60px',
  },
  gridContainer: {
    padding: '20px',
  },
  card: {
    minHeight: '100%',
    display: 'flex',
    flexDirection: 'column',
  },
}));
